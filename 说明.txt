ng+PHP7.2 + MySQL 5.6.50

application/database.php
运行目录public 伪静态 thinkphp
/hzw-system-login admin    aa123456

app.php配置文件中有时差设置

cp config/app.example.php config/app.php
注意：
    1. 配置文件在config/app.php

后端配置

 add_header 'Access-Control-Allow-Credentials' 'true';
   # add_header 'Access-Control-Allow-Origin' 'https://kele1006.space/home';
    add_header 'Access-Control-Allow-Headers' 'language,token,access-control-allow-credentials,access-control-allow-origin';

前端测试环境必看：
        把src/request/index.js中的baseURL改为后台地址baseURL: 'https://admin.amazon90.com/index/', // 直接请求服务器，不走代理
        在后端nginx配置文件中添加上面的服务器配置，加到server{}里面，root下面就可以了，可以解决本地代理请求接口速度过慢和直接写后端接口跨域的问题

自动冻结订单:http://www.jake1006.space/index/Crontab/freeze_order

前端伪静态
location ~ ^/vi {    # 使用 ^/vi 确保匹配以 /vi/ 开头的路径
    proxy_pass http://43.156.84.195:5544;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header REMOTE-HOST $remote_addr;
    
    # 允许跨域请求
    add_header Access-Control-Allow-Origin * always;
    add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS' always;
    add_header Access-Control-Allow-Headers '*' always;
    
    if ($request_method = 'OPTIONS') {
        return 204;
    }
}

location / {
  root   /www/wwwroot/qianduan.com;    #地址弄自己的地址
  try_files $uri $uri/ /index.html;
  index  index.html index.htm;
}

后改独立前段规则
location ~ ^/(vi|upload)/ {
        proxy_pass http://127.0.0.5;
        proxy_buffer_size 16k;
        proxy_buffers 4 32k;
        proxy_busy_buffers_size 64k;
        
        proxy_ssl_verify off;
        proxy_ssl_server_name on;
        
        proxy_connect_timeout 10s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        
        proxy_set_header Host admin.gnvcso.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
  location / {
        try_files $uri $uri/ /index.html;
    }
# 0. 锁定
chattr +i app.js

lsattr app.js 查看是否锁
# 1. 解除锁定
chattr -i app.js

# 2. 覆盖新文件
上传新dist

# 3. 再锁
chattr +i app.js

实名认证说明：

0只展示信息上传
1信息审核中，可以选择2或者3
2基础认证通过，打开高级认证输入
3信息不通过
4高级认证审核，可以选择5或者6
5通过高级认证
6拒绝高级认证


后台对应
为1时选择2或者3
为4时选择5或者6


提交姓名变1，审核通过后变2，审核拒绝后变3，重新提交后又变1


